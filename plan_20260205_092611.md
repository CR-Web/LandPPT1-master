我将修改 `src/landppt/api/global_master_template_api.py` 中的 `detect_slots_business_blue_01` 函数，以精确识别 `business_blue_01` 模板的槽位，解决元素缺失和页面类型识别不全的问题。

### 实施计划

1.  **优化封面页识别**：
    -   改进“汇报人”和“日期”的关键词匹配逻辑，确保能兼容中文全角冒号（`：`）等标点符号差异。
    -   校准位置计算逻辑，确保槽位框精确贴合文本框边界。

2.  **实现目录页（TOC）识别**：
    -   添加通过“目录”或“CONTENTS”关键词识别目录页的逻辑。
    -   识别占位符文本“在此输入您的标题...”为目录项标题（`toc_item_title`）。
    -   识别占位符文本“Enter your content h...”为目录项描述（`toc_item_desc`）。
    -   根据垂直位置（`top`）对识别出的项目进行排序，分配顺序 ID（如 `toc_item_1`, `toc_item_2`）。

3.  **实现结束页识别**：
    -   添加通过“THANKS”或“输入您的LOGO”关键词识别结束页的逻辑。
    -   将“THANKS”相关文本映射为结束页标题（`conclusion_title`）。
    -   识别结束页上的汇报人和联系方式信息。

4.  **实现内容页识别**：
    -   添加通用内容页的识别逻辑。
    -   将“TITLE HERE”映射为页面标题（`page_title`）。
    -   将大段占位文本（如“The input here...”、“单击此处添加文本...”）映射为内容主体（`content_body`）或列表项。

### 技术细节

-   **修改文件**：`e:\workandstudy\LandPPT-master\LandPPT-master\src\landppt\api\global_master_template_api.py`
-   **修改函数**：`detect_slots_business_blue_01`
-   **逻辑流程**：
    -   首先遍历幻灯片上的形状，根据特征关键词确定页面类型（封面、目录、结束页、内容页）。
    -   确定页面类型后，应用该类型专属的槽位提取规则。
    -   将提取到的形状坐标（`left`, `top`, `width`, `height`）转换为百分比，作为槽位的 `position` 参数。
