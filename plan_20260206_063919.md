I will fix the issue where the generated PPT template style does not match the selected template (specifically for imported templates like `business_blue_01`).

The root causes identified are:

1. **Style Deletion**: The `_process_template_html` function aggressively deletes `<style>` tags when it detects placeholders, which removes critical global styles for imported templates.
2. **Forced Background Overwrite**: The `_ensure_fixed_canvas_size` function injects CSS that forces the `body` background to transparent and the `slide` background to white, potentially hiding the original template background.

**Plan:**

1. **Modify** **`src/landppt/services/enhanced_ppt_service.py`**:

   * Update `_process_template_html` to detect if the template is an imported PPT (using markers like `data-slide-index`, `ppt-deck`). If it is, skip the deletion of `<style>` tags to preserve original styling.

   * Update `_ensure_fixed_canvas_size` to accept an `is_imported` flag. When true, avoid injecting intrusive background styles (`background-color: #fff`) that might conflict with the template's design.

   * Update `_generate_slide_with_template` to pass the `is_imported` flag when calling `_ensure_fixed_canvas_size`.

2. **Verification**:

   * I will verify the code changes by checking that `<style>` tags are preserved and forced background styles are omitted for imported templates.

   * I will rely on your visual confirmation after the fix, as I cannot see the rendered output directly.

